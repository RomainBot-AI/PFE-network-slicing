mettre le dataset (.csv) dans le dossier mininet 

docker compose up

new terminal (same directory)

-> docker exec -it mininet bash
-> python3 topology.py


new terminal

-> python3 ppo.py 
(might need torch -> pip install torch)
